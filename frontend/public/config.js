// Optional runtime config — edit and redeploy without changing env or rebuilding
// the React bundle (this file is served as a static asset from /config.js).
// Set your Northflank (or any) API origin here, no trailing slash.
window.__EXDAV_BACKEND_URL__ = "https://p01--exdav--2rlz8sczq2qc.code.run";
